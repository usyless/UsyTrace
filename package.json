{
  "name": "uk.usyless.usytrace",
  "version": "v1.0",
  "description": "browser based frequency response tracer",
  "homepage": "https://github.com/usyless/UsyTrace",
  "bugs": {
    "url": "https://github.com/usyless/UsyTrace/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/usyless/UsyTrace.git"
  },
  "license": "AGPL-3.0",
  "author": "usy",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:debug": "node scripts/build.js --debug",
    "build:wasm": "node scripts/build.js --wasm",
    "build:wasm:debug": "node scripts/build.js --wasm --debug",
    "build:js": "node scripts/build.js --js",
    "build:js:debug": "node scripts/build.js --js --debug",
    "build:css": "node scripts/build.js --css",
    "build:css:debug": "node scripts/build.js --css --debug",
    "build:dist": "node scripts/build.js --dist",
    "serve": "npm run scripts/build && node scripts/server.js",
    "serve:debug": "npm run scripts/build:debug && node scripts/server.js"
  },
  "devDependencies": {
    "express": "^5.2.1"
  }
}
