{
  "name": "uk.usyless.usytrace",
  "version": "v1.0",
  "description": "browser based frequency response tracer",
  "homepage": "https://github.com/usyless/UsyTrace",
  "bugs": {
    "url": "https://github.com/usyless/UsyTrace/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/usyless/UsyTrace.git"
  },
  "license": "AGPL-3.0",
  "author": "usy",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:debug": "node build.js --debug",
    "build:wasm": "node build.js --wasm",
    "build:wasm:debug": "node build.js --wasm --debug",
    "build:js": "node build.js --js",
    "build:js:debug": "node build.js --js --debug",
    "build:css": "node build.js --css",
    "build:css:debug": "node build.js --css --debug",
    "build:dist": "node build.js --dist",
    "serve": "npm run build && node server.js",
    "serve:debug": "npm run build:debug && node server.js"
  },
  "devDependencies": {
    "express": "^5.2.1"
  }
}
